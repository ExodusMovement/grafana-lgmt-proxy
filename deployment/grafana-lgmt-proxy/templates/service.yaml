apiVersion: v1
kind: Service
metadata:
  name: {{ include "grafana-lgmt-proxy.fullname" . }}
  labels:
    {{- include "grafana-lgmt-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "grafana-lgmt-proxy.selectorLabels" . | nindent 4 }}
